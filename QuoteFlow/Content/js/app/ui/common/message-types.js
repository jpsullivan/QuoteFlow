"use strict";

/**
 * An enum specifying the different AUI message types and their renderers.
 */
var MessageTypes = {
	WARNING: JST["common/warning-message"],
	ERROR: JST["common/error-message"],
	SUCCESS: JST["common/success-message"]
};

module.exports = MessageTypes;
