@model QuoteFlow.Api.Models.ViewModels.Users.Manage.AccountSettings

@{
    Layout = "../Shared/User/_ManageLayout.cshtml";
    ViewBag.Title = "Account settings - QuoteFlow";
}

<header class="aui-page-header">
    <div class="aui-page-header-inner">
        <div class="aui-page-header-main">
            <h2>Account settings</h2>
        </div>
    </div>
</header>

@using (Html.BeginForm("SaveAccountSettings", "Users", FormMethod.Post, new { @class = "aui" }))
{
    <div class="field-group">
        @Html.LabelFor(m => m.FullName)
        @Html.TextBoxFor(m => m.FullName, new {@class = "text"})
        <div class="error">@Html.ValidationMessageFor(m => m.FullName)</div>
    </div>
    <div class="field-group">
        @Html.LabelFor(m => m.EmailAddress)
        @Html.TextBoxFor(m => m.EmailAddress, new {@class = "text"})
        <div class="error">@Html.ValidationMessageFor(m => m.EmailAddress)</div>
    </div>

    <div class="buttons-container form-footer">
        <div class="buttons">
            <input accesskey="s" class="aui-button aui-button-primary" id="account-settings-submit" name="account-settings-submit" title="Press Alt+s to submit this form" type="submit" value="Update profile">
        </div>
    </div>
}
